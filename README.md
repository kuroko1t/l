# ls
This library is ls command that displays the full path of a file.

* show full path
* show git branch name

## Example

```bash
$ go run main.go
../dcp(master)
../gkill(master)
../ls(show_detail)
../venv
../tmp
```
